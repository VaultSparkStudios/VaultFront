<!-- generated-by: render-audit-md.mjs — DERIVED FROM AUDIT_2026-08-05.json; do not hand-edit -->

# Project Audit — VaultFront

> Source of truth: the JSON sidecar. Edit JSON, then re-render.

## Ranked Plan

| #   | Tier | Axis                                                     | Effort | Impact | Innov | Priority | Status | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | :--: | -------------------------------------------------------- | ------ | :----: | :---: | :------: | :----: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 148 |  🔥  | security / coherence / public boundary                   | 1h     |   8    |   6   |   91.0   |   ✓    | **retire-public-obelisk-broker-copy** — Remove the orphaned Studio Ops secret broker from the public repo. scripts/lib/obelisk-broker.mjs is tracked, unused, and copied from Studio Ops. It references private portfolio policy, receipt paths, grant issuer keys, and operator trust semantics that do not belong in this public deployable repository; the canonical filename sanitizer does not currently recognize this code-level boundary leak. **Recipe:** Delete the orphaned broker without touching the project secrets gateway; add a public-boundary regression that forbids Studio Ops policy/receipt paths and the broker filename in tracked deployable code; run the canonical public sanitizer and secret scan; keep native Obelisk relying-party work externally blocked until registered origin/callback evidence exists. Core-loop step: player/operator input → trusted public surface → safe participation reward → identity progression.                                                                                                                                                                                                                                                              |
| 145 |  🔥  | speed / organization / token reduction                   | 2h     |   8    |   7   |   84.0   |   ✓    | **project-local-skill-bridge** — Make required Studio skills executable from the project root. Both studio-start and audit required project-local skill-profile and sample-codebase commands, but each failed with MODULE_NOT_FOUND in the fresh Session 95 preflight even though the canonical implementations exist in Studio Ops. The missing bridge forces manual fallbacks and silently drops medium-specific success bars. **Recipe:** Extend the existing allowlisted control-plane wrapper with project-scoped sample-codebase and non-project-flag skill-profile adapters; preserve cwd provenance; add adversarial tests for missing control plane, injected root, passthrough arguments, and denied unknown tools; then prove both exact skill commands succeed from VaultFront. Core-loop step: operator input → verified plan → shipped safeguard → compounding product quality.                                                                                                                                                                                                                                                                                                                                 |
| 147 |  🔥  | security / speed / observability                         | 3h     |   8    |   8   |   83.0   |   ✓    | **asset-aware-secret-signal** — Preserve secret blocking while eliminating asset entropy noise. The canonical full-tree scan emits 1,974 findings, all low-confidence matches inside existing base64 SVG, flag, cosmetic, and copied static assets, with zero medium/high findings. A signal this noisy is unusable for sanitization review and can hide the one real credential that matters. **Recipe:** Classify entropy-only patterns separately from semantic credential patterns; suppress low-confidence random-token matches only inside verified asset payload contexts and generated resource mirrors; keep every medium/high pattern blocking everywhere; deduplicate copied static/resource evidence; expose confidence counts; and add fixtures proving real keys in source or SVG text still fail. Core-loop step: security input → trusted build/release fantasy → clean evidence reward → safe progression.                                                                                                                                                                                                                                                                                                   |
| 149 |  🔥  | security / reliability / coverage                        | 3h     |   9    |   7   |   81.0   |   ✓    | **bounded-identity-introspection** — Bound remote identity introspection during game admission. getUserMe directly awaits the issuer users/@me fetch with no deadline, abort signal, or focused test coverage. A slow or wedged identity provider can hold WebSocket admission indefinitely even though every subsequent authorization branch waits on this result. **Recipe:** Add an explicit short admission deadline with AbortController ownership and exact timer cleanup; inject fetch for deterministic tests; normalize client-visible failure while retaining safe operator diagnostics; prove success, non-200, invalid schema, network error, and never-resolving provider behavior; keep existing EdDSA issuer/audience verification unchanged. Core-loop step: player login input → bounded match admission → playable fantasy reward → account progression.                                                                                                                                                                                                                                                                                                                                                    |
| 146 |  ⚡  | speed / organization / observability                     | 2h     |   8    |   6   |   72.0   |   ✓    | **idempotent-doctor-closeout-format** — Make doctor sidecar closeout byte-idempotent. project-doctor writes audits/doctor-latest.json with JSON.stringify formatting, while closeout-autopilot formats only STARTUP_BRIEF and PROJECT_STATUS before staging. The pre-commit hook then rewrites the sidecar, so every closeout starts with avoidable byte churn and the closeout runner violates the Session 92 decision that it formats every generated truth surface itself. **Recipe:** Include the doctor sidecar in the closeout generated-format step, verify its canonical content digest remains stable across whitespace changes, and add a regression that runs the formatter path twice and observes no second diff. Preserve the content-canonical evidence hash contract rather than hashing file bytes. Core-loop step: operator verification input → trustworthy evidence → confident shipment → reliable progression.                                                                                                                                                                                                                                                                                          |
| 150 |  ⚡  | speed / organization / developer experience              | 4h     |   7    |   6   |   49.0   |   ✓    | **single-owner-static-assets** — Give static assets one Vite ownership path. The production and browser-proof builds emit thousands of warnings because resources is simultaneously Vite publicDir and an import source. The config comment claims public assets may be imported, but Vite explicitly rejects that ownership model; real warnings are buried under repeated path guidance. **Recipe:** Disable publicDir ownership and copy resources through the already-installed vite-static-copy pipeline while leaving imported assets under normal module ownership; prove dev URLs, SPA pages, service worker, maps, language files, sprites, and media remain byte-reachable; add a warning-budget smoke that fails if the public-directory warning class returns; re-run bundle and rendered-pixel gates. Core-loop step: page-load input → immediate battlefield fantasy → responsive feedback reward → session progression.                                                                                                                                                                                                                                                                                        |
| 135 |  🔥  | security / feedback loop / AI / observability            | 5h     |   10   |   9   |   96.0   |   ✓    | **server-certified-narration-spine** — Make live narration inherit server-certified match authority. NarratorReporter lets any browser POST an arbitrary activity and label for any gameId into NarratorBus. The server validates shape and IP rate only; it does not verify actor membership, live game ownership, or that the event occurred. GameServer imports NarratorBus only to close it, so AI commentary can be driven by browser-authored fiction. **Recipe:** Project a privacy-minimal narration event only after GameServer accepts authoritative intents and certified lifecycle outcomes; remove the public narrator event mutation and client reporter; preserve bounded SSE subscription; define a typed event catalog with no actor identifiers; lower the public-ingest ceiling; and prove fake game IDs, rejected intents, duplicate events, and browser posts cannot trigger commentary.                                                                                                                                                                                                                                                                                                                 |
| 136 |  🔥  | security / speed / organization / token reduction        | 6h     |   10   |   8   |   82.0   |   ✓    | **runtime-feature-reachability-closure** — Give every runtime feature authority a reachable consumer or retire it. Worker exposes multiple feature surfaces with no client, agent, or operator consumer: match-mission, match-commentary, bot-lore, match-coach, clan-war, and parallel Intel market routes. The Intel routes accept caller identities, keep unbounded game maps, return random fallback risk, advertise a 2,000-gold cost, and never mutate the certified economy. Unused Api helpers also preserve phantom battle-narrative and bot-persona authority. **Recipe:** Create an executable runtime-feature reachability catalog that binds each admitted route to a source consumer and audience. Remove unreachable routes, imports, stores, client helpers, policies, and tests; keep core in-match Intel commands as the sole economy authority; forbid random quote data and uncharged cost claims; ratchet Worker and public-ingest ceilings downward; and mutation-test reintroduction.                                                                                                                                                                                                                  |
| 137 |  🔥  | UX / AI / reliability / accessibility                    | 4h     |   9    |   8   |   72.0   |   ✓    | **session-bound-prematch-intelligence** — Make pre-match intelligence bounded, abortable, and stale-session-proof. GameStartingModal launches three provider-backed requests with Promise.all and owns neither an AbortController nor a session generation. hide() leaves requests and two timeouts alive; an earlier match can overwrite a later modal, and the fixed 300px surface can overflow when all three cards arrive on mobile. There is no focused lifecycle test. **Recipe:** Introduce a generation-scoped pre-match session with AbortController, bounded per-task deadlines, all-settled status, timer cleanup, stale-result rejection, and exact hide/disconnect teardown. Render an accessible responsive shell with deterministic loading/degraded states, validate provider payloads before projection, and capture desktop/mobile evidence across all themes.                                                                                                                                                                                                                                                                                                                                              |
| 138 |  ⚡  | UX / organization / release / accessibility              | 3h     |   8    |   7   |   55.0   |   ✓    | **single-public-route-graph** — Generate static and in-app public navigation from one route graph. public/footer-manifest.json generates and validates ten static pages, but the Lit application footer repeats routes, copyright, brand, and attribution by hand. It omits the required Play destination and adds Wiki/Source links outside the checked graph, so the green footer gate does not cover the actual app footer. **Recipe:** Make one typed public route manifest drive static shell generation and the Lit footer projection, explicitly classify app-only external links, and extend the scoped checker to validate the application consumer. Preserve ordering, labels, legal pages, branding, upstream attribution, and current visual output; add digest/reachability tests and close the local half of the aged SIL item without claiming staging screenshots.                                                                                                                                                                                                                                                                                                                                            |
| 139 |  🔥  | UX / reliability / feedback loop / accessibility         | 4h     |   9    |   8   |   72.0   |   ✓    | **player-visible-transport-recovery** — Turn transport recovery evidence into calm player-visible state. Transport now emits exact connection state, retry attempt, outbox depth, and overflow events, but only tests consume them. Players see no waiting/recovering/synchronized feedback, queued actions are invisible, and protocol refusal still uses blocking alert(), bypassing the app's accessible modal and theme system. **Recipe:** Add a dedicated recovery presenter subscribed to transport state/overflow events: a non-blocking aria-live banner for waiting/recovering/synchronized states, queued-action count and reject-newest overflow guidance, and an accessible fatal-refusal modal. Keep transient states generation-safe, never claim synchronized before rejoin succeeds, test event-to-view semantics, and capture all themes at desktop/mobile widths.                                                                                                                                                                                                                                                                                                                                          |
| 140 |  🔥  | UX / gamification / performance / feedback loop          | 5h     |   10   |   8   |   94.0   |   ✓    | **match-bound-first-contact** — Make first contact begin inside the match fantasy and recover transfer headroom. Main.ts statically imports and mounts VaultFrontTutorial during lobby bootstrap. Its 800ms timer opens instructions for an in-match tracker that does not exist yet, and dismissing it permanently consumes the versioned seen state. The lobby hero simultaneously exposes internal visual-pass/changelog language. The same eager surface contributes to a current-main CI failure: initial Brotli entry is 594,207 bytes against a 592,619-byte ceiling. **Recipe:** Replace timer-driven lobby mounting with one match-ready signal emitted only after authoritative VaultFront status reaches the mounted ControlPanel. Lazy-load the tutorial behind that signal, preserve exactly-once shown/skip/complete telemetry, cancel listeners and timers on teardown, and rewrite the lobby hero around the Capture → Convoy → Pressure → Breach fantasy. Prove no lobby display/seen mutation, one match display, teardown safety, and a directly green unchanged bundle ceiling; capture the touched lobby and in-match states across every theme at desktop/mobile widths.                                |
| 141 |  🔥  | game loop / observability / UX / correctness             | 5h     |   10   |   9   |   90.0   |   ✓    | **personal-first-extraction-authority** — Give First Extraction a server-derived personal agency ledger. Vault Pressure is player-or-team scoped and already projects contributors by actor, but ControlPanel passes the shared pressure state into advanceFirstExtractionProgress. That projector promotes pressureStarted into vaultCaptured and convoyAction and promotes team victory into decisiveDelivery, so a teammate can complete the learner's personal capture, convoy, and winning-delivery steps. **Recipe:** Separate team context from personal evidence. Recover personal capture from actor activity or controlled-site state, convoy engagement from actor activity or the contributor ledger, personal Pressure from contributors, and decisive delivery only from the exact vault_breach_victory actor. Let team threshold/window state advance only the explicitly team-labeled step after personal participation. Preserve reconnect recovery from status, forbid retroactive teammate credit, expose a compact personal/team evidence receipt for the tracker, and add solo/team/reconnect/adversarial tests plus rendered states.                                                                    |
| 142 |  🔥  | feature depth / retention / gamification / security      | 7h     |   9    |   9   |   82.0   |   ✓    | **mastery-doctrine-vault** — Turn certified Mastery into a player-chosen non-power Doctrine Vault. Certified Daily Mastery awards durable currency and the live sidebar displays only '<balance> M total'. There is no catalog, entitlement threshold, selection, spend path, or downstream consumer, so the retention hook accumulates obligation without aspiration. The wallet already has transactional PostgreSQL and process-local parity, making a bounded non-power choice layer feasible without retuning gameplay. **Recipe:** Add a versioned Doctrine catalog of cosmetic/coaching identities with no combat-stat effects. Implement an authenticated, rate-limited, replay-safe select-or-unlock transaction that atomically deducts Mastery once, persists entitlements and the active Doctrine in PostgreSQL with process-local parity, and returns an explicit receipt. Surface next unlock, owned choices, active Doctrine, durability, and insufficient-funds truth in the sidebar; use the selected Doctrine only to frame existing coaching/next-match goals. Add route-policy coverage, migration, concurrent/idempotent store tests, client schema validation, and desktop/mobile/theme rendered proof. |
| 143 |  ⚡  | observability / organization / token reduction           | 2h     |   8    |   7   |   66.0   |   ✓    | **actionable-genius-brief-truth** — Make the startup hit list distinguish actionable work from shipped history. generate-genius-list maps shipped audit items to done, but renderBrief prints the first entries regardless of status. The S94 startup brief therefore announces Unblocked 0 while presenting five checked completed items under GENIUS HIT LIST, and /start surfaces a completed item as the top action. **Recipe:** Render only actionable entries in the hit list. When none exist, emit a source-derived exhaustion tile with shipped/pending/blocked totals, latest audit identity, and the next expansion command; keep full completed history in GENIUS_LIST.md/cache. Add tests for mixed, exhausted, blocked-only, and malformed sidecars, then re-render and validate the canonical brief.                                                                                                                                                                                                                                                                                                                                                                                                           |
| 144 |  ⚡  | observability / speed / token reduction / organization   | 3h     |   8    |   8   |   62.0   |   ✓    | **doctor-evidence-sidecar** — Separate compact project health from full doctor evidence. PROJECT_STATUS.json embeds all thirteen doctor checks including duplicated child-process commands, stdout payloads, timestamps, and parsed data. Every startup/status consumer pays for the full transcript even though normal rendering uses only aggregate counts; warning ownership is the only path that needs check detail. **Recipe:** Write the complete doctor report to one atomic hash-bound audits/doctor-latest.json sidecar and keep only aggregate counts, observation time, source, evidence path, and SHA-256 digest in PROJECT_STATUS. Teach startup warning classification to load and verify the sidecar only when warnings/failures exist, fail closed on missing or mismatched detail, and retain backwards-compatible reads for legacy embedded checks. Add tamper/missing/legacy/green-path tests and measure the status-token reduction.                                                                                                                                                                                                                                                                     |
| 151 |  🔥  | speed / organization / reliability / game loop           | 6h     |   10   |   8   |  171.0   |   ✓    | **deterministic-match-session-teardown** — Make leaving a match end every match-owned execution surface. joinLobby drops currentGameRunner without calling stop. ClientGameRunner.stop omits renderer, input, touch, and EventBus teardown; GameRenderer owns an unbounded requestAnimationFrame loop and anonymous resize/context handlers; InputHandler owns a 1ms interval plus anonymous global listeners. A rematch can therefore retain workers, renders, keys, timers, and callbacks from prior play. **Recipe:** Give EventBus a reversible listener checkpoint, make ClientGameRunner.stop idempotently restore its match scope, call stop before discarding the runner, replace the 1ms input loop with one animation-frame cadence, and add explicit destroy/dispose paths for renderer/input/touch/layers with lifecycle tests proving a leave/rematch returns worker, timer, animation, and listener counts to baseline. Core-loop step: player input → territorial command fantasy → match result → clean rematch progression.                                                                                                                                                                                  |
| 155 |  🔥  | feedback loop / UX / feature depth / analytics           | 5h     |   9    |   8   |  158.0   |   ✓    | **causal-certified-match-feedback** — Turn two ratings into privacy-minimal causal playtest evidence. The certified feedback surface records only match and map scores. MatchFeedbackStore already retains an optional comment field, but the UI never sends it and summaries never expose it, so a low score cannot distinguish pacing, clarity, agency, comeback, map-flow, or technical friction. **Recipe:** Add one optional bounded cause tag chosen from a project-owned taxonomy, validate it server-side, store it through the existing feedback row without free-form player text, aggregate it as a certified cohort, include it in the accepted receipt, and render accessible 44px choices after both ratings. Tests must prove certificate binding, duplicate behavior, retention, aggregation, and stale-game isolation. Core-loop step: match input → decisive convoy fantasy → certified reflection reward → evidence-led progression tuning.                                                                                                                                                                                                                                                                |
| 152 |  🔥  | security / UX / observability / soul fidelity            | 3h     |   9    |   7   |  150.0   |   ✓    | **truthful-multitab-collision** — Replace fabricated surveillance theater with a truthful local tab-collision receipt. MultiTabModal generates a fake IP and fake fingerprint, labels the state RECORDING and Reported TRUE, and threatens suspension although MultiTabDetector only observes a same-origin localStorage lock. stopMonitoring removes different bound function objects, so its listeners survive teardown; repeated show calls can also stack countdown intervals. **Recipe:** Delete fabricated identity/reporting claims, present the exact local same-browser collision scope and non-server evidence boundary, retain a calm bounded action pause, use stable handler references, clear penalty timers, make start/stop idempotent, and add accessible lifecycle tests plus desktop/mobile/theme screenshots. Core-loop step: player input → fair-command fantasy → trustworthy interruption receipt → safe return to match progression.                                                                                                                                                                                                                                                                  |
| 154 |  🔥  | AI / security / capital / token-API reduction            | 4h     |   9    |   7   |  142.0   |   ✓    | **retire-browser-authored-remote-microhint** — Retire the unauthenticated browser-authored remote micro-coach path. CoachHintEngine already renders an immediate deterministic local tactical hint, but an opt-in calls GET /api/vaultfront/micro-hint with browser-authored gold/sites. The provider route has no actor admission, no certified match evidence, no server cache, and is absent from the remote-reservation ordering contract, while every accepted call consumes metered provider capacity. **Recipe:** Remove the remote route, client fetch helper, opt-in storage switch, and unreachable provider prompt while retaining deterministic local coaching and its telemetry; add runtime tombstones and cost-firewall tests forbidding the route/provider literal from returning. Do not relabel canonical certified recap/debrief AI. Core-loop step: tactical input → coach-supported convoy fantasy → immediate local clarity reward → skill progression without variable-cost abuse.                                                                                                                                                                                                                     |
| 157 |  🔥  | retention / reliability / UX / gamification              | 4h     |   9    |   7   |  139.0   |   ✓    | **generation-safe-progression-debrief** — Make progression debrief repeatable and generation-safe across rematches. ProgressionDebrief is a singleton Layer whose requested flag never resets when createRenderer binds a new game. Its polling checks cancelled only before fetch, clears a wait timer without resolving the pending promise, and can apply a prior games receipt after a new match begins. The first post-match debrief can therefore suppress or overwrite later rematch progression. **Recipe:** Bind the component through an explicit game-generation reset, abort/resolve every wait and fetch boundary, reject receipts after generation change, reset claim/loading/certification state, and add consecutive-match plus stale-response tests. Core-loop step: match result → certified reward debrief → chosen rematch goal → next-match progression.                                                                                                                                                                                                                                                                                                                                                |
| 156 |  ⚡  | gamification / retention / feature depth / soul fidelity | 4h     |   8    |   8   |  132.0   |   ✓    | **doctrine-guided-rematch-continuity** — Carry the active non-power Doctrine through debrief and the next rematch goal. The Doctrine Vault persists a server-owned active coaching identity, but only GameRightSidebar renders it. ProgressionDebrief chooses a generic Convoy Mastery goal without loading the active Doctrine, and the rematch event carries only generic text/evidence, so the paid non-power choice disappears at the exact progression-to-next-input seam. **Recipe:** Load the certified daily-mastery snapshot alongside the progression dividend, derive a bounded Doctrine coaching frame from the server-owned catalog, show it in the debrief without changing combat stats, and bind its id/brief into the rematch goal receipt with stale-session guards. Core-loop step: certified reward → chosen commander identity → doctrine-framed rematch input → long-term mastery progression.                                                                                                                                                                                                                                                                                                          |
| 153 |  ⚡  | UX / accessibility / security / conversion               | 3h     |   8    |   4   |   91.0   |   ✓    | **accessible-account-recovery-state** — Replace blocking account-recovery alerts with bounded accessible state. AccountModal magic-link recovery uses three window.alert calls, has no pending guard, and leaves the button active during the request. Keyboard and screen-reader users lose modal context, rapid activation can duplicate requests, and success/error truth is detached from the themed account surface. **Recipe:** Add semantic inline validation, pending, success, and retry states with aria-live/role alert, disable duplicate submission, preserve the entered address, reset request state on a new modal session, and cover success/failure/rapid-click/keyboard behavior plus all-theme desktop/mobile rendering. Core-loop step: identity input → trusted account gateway → recovery confirmation reward → return-to-play progression.                                                                                                                                                                                                                                                                                                                                                            |
| 158 |  🔥  | reliability / release architecture / game loop           | 8h     |   10   |   9   |  170.0   |   ✓    | **match-safe-blue-green-promotion** — Make deployment preserve live match generations. update.sh force-removes the live container before starting its replacement, bypassing the worker graceful-shutdown path and terminating active WebSockets and matches. A graceful worker implementation is not a graceful deployment when transport uses a hard kill. **Recipe:** Start and health the replacement digest before changing admission, drain the old generation through a bounded readiness state, preserve sticky live WebSockets until active games reach zero, then retire the old container and retain a digest-bound promotion/drain receipt. Core-loop step: live player input → uninterrupted match fantasy → certified result reward → deployment-safe progression.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 159 |  🔥  | feedback loop / persistence / reliability                | 8h     |   10   |   8   |  165.0   |   ✓    | **durable-certified-archive-outbox** — Make certified archive delivery durable and observable. Certification fire-and-forgets archive delivery; Archive has no deadline, retry, idempotent receipt, or propagated failure, while end() treats certificate presence as archive success. Certified records can disappear silently and incomplete termination snapshots share the same path. **Recipe:** Separate certified and incomplete archive types, return typed delivery results, enforce deadlines, queue idempotent retry keyed by certificate/game, expose readiness counters, and admit certified downstream consumers only after a delivery receipt. Core-loop step: match result → certified archive fantasy → durable history reward → replay/rematch progression.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 160 |  🔥  | progression / persistence / reliability                  | 6h     |   9    |   7   |  145.0   |   ✓    | **authoritative-achievement-rehydration** — Rehydrate achievements before authenticated progression reads. AchievementStore creates process-local state, its hydrateFromDb method has no production caller, meta-chain unlocks bypass the persistent path, and authenticated reads return the empty in-memory view after restart despite a database-backed status claim. **Recipe:** Hydrate each actor once before reads/progression, persist meta unlocks through the canonical path, singleflight concurrent loads, bound cache lifetime, expose degraded state, and test restart, concurrency, persistence failure, meta-chain, and multi-worker parity. Core-loop step: certified action → achievement recognition fantasy → durable unlock reward → long-term progression.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 161 |  🔥  | observability / release truth / organization             | 5h     |   9    |   8   |  140.0   |   ✓    | **complete-state-scope-inventory** — Make persistence scope inventory complete by construction. StateScopeLedger validates only a fixed ten-entry array and omits live durable/process-fallback stores, so its green release verdict can be internally consistent yet incomplete. **Recipe:** Register every runtime store capability once in a machine-readable authority used by constructors, readiness, and tests; fail closed when an exported or mounted store is missing; classify release criticality and preserve explicit fallback truth. Core-loop step: player action → durable-system fantasy → trustworthy state reward → restart-safe progression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 162 |  ⚡  | security / privacy / observability                       | 1.5h   |   8    |   5   |  120.0   |   ✓    | **redact-turnstile-capability-tokens** — Keep Turnstile admission capabilities out of browser logs. The browser logs the complete Cloudflare Turnstile response token before verification, exposing a live admission capability to consoles, screenshots, extensions, and log collectors. **Recipe:** Delete token-bearing output, retain only non-sensitive lifecycle metadata, and add a source-negation regression for interpolated authentication and challenge tokens. Core-loop step: join input → trusted admission fantasy → verified play reward → safe match progression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 163 |  🔥  | AI / reliability / token-API reduction                   | 5h     |   8    |   7   |  138.0   |   ✓    | **single-bounded-remote-ai-executor** — Route every remote AI edge through one bounded executor. Oracle, recap, and coach use the bounded executor, but prophecy, dynasty, and prematch call the provider SDK directly after reserving quota; abandoned or slow calls can retain server work and spend capacity. Prophecy prompts on mutator while its cache key omits it. **Recipe:** Route every provider call through reserve-execute-validate-receipt, include all prompt-shaping fields in cache keys, forward request abort, expose timeout/cancel evidence, and make direct SDK calls outside the helper fail CI. Core-loop step: certified state → optional intelligence fantasy → bounded insight reward → informed progression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 164 |  🔥  | gamification / retention / feature depth                 | 8h     |   9    |   9   |  160.0   |   ✓    | **reachable-doctrine-match-spine** — Make Doctrine rematch intent reach the next live match. ProgressionDebrief emits a Doctrine-bound rematch event, but production only binds the game generation and no runtime consumer handles the event; the prior completion claim is therefore test-only at the progression-to-input seam. **Recipe:** Consume the rematch event in production, bind its certified Doctrine id/brief to the next match generation, let it frame local coaching without changing statistics, and render a certified post-match Doctrine Review with stale-session, accessibility, mobile, and theme proof. Core-loop step: certified reward → chosen commander identity → doctrine-framed next-match input → mastery progression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 165 |  🔥  | feedback loop / UX / soul fidelity                       | 6h     |   9    |   8   |  150.0   |   ✓    | **cause-bound-local-coach** — Bind local coaching to deterministic execution causes. The local coach maps comeback_surge to chain_broken even though chain resets occur on independent invalid or expired transitions, so the visible explanation can fabricate causality. **Recipe:** Emit typed reset reasons from deterministic simulation, render an explainable what/why/next coaching receipt with a focus action, and certify the reason into post-match evaluation without accepting browser-authored causes. Core-loop step: tactical input → deterministic consequence fantasy → explainable coaching reward → skill progression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 166 |  ⚡  | UX / accessibility / feature depth                       | 5h     |   8    |   7   |  118.0   |   ✓    | **accessible-reroute-decision-matrix** — Turn reroute projections into an accessible decision matrix. Five rich reroute projections render as sub-44px ungrouped buttons without pressed state or decision semantics, so keyboard, touch, and assistive-technology users cannot reliably compare the advanced command surface. **Recipe:** Expose a keyboard-operable radio group with 44px targets, pressed/selected semantics, live ETA-risk-reward deltas, Safe/Fast/Value summaries, and full matrix disclosure; preserve exact simulation authority and add desktop/mobile/all-theme proof. Core-loop step: route input → commander decision fantasy → legible tradeoff reward → tactical progression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## Depth Ladder

### retire-public-obelisk-broker-copy

- **L1** — Delete the unused broker copy.
- **L2** — Add source-negation tests for private control-plane paths.
- **L3** — Executable public-boundary guard, sanitizer proof, and explicit native-identity non-substitution.

### project-local-skill-bridge

- **L1** — Add minimal wrappers for the two missing commands.
- **L2** — Inject exact project root and preserve profile cwd semantics.
- **L3** — Allowlist, provenance, negative-path tests, and executable start/audit smoke proof.

### asset-aware-secret-signal

- **L1** — Skip low-confidence entropy matches inside base64 image payloads only.
- **L2** — Deduplicate generated resource mirrors and report confidence counts.
- **L3** — Context classifier, adversarial real-key fixtures, exact zero-noise full-tree receipt, and staged-scan parity.

### bounded-identity-introspection

- **L1** — Apply an AbortController deadline to users/@me.
- **L2** — Inject fetch and cover all response/error branches.
- **L3** — Leak-safe normalized receipt, timer-cleanup proof, admission-latency contract, and Worker integration assertion.

### idempotent-doctor-closeout-format

- **L1** — Format audits/doctor-latest.json in closeout-autopilot.
- **L2** — Assert content digest survives formatting and the summary reloads.
- **L3** — Two-pass byte-idempotency regression plus generated-surface ownership contract.

### single-owner-static-assets

- **L1** — Move resources copy ownership into vite-static-copy.
- **L2** — Verify dev/build URL parity for direct and imported assets.
- **L3** — Zero-warning contract, artifact reachability matrix, bundle budgets, and desktop/mobile visual recapture.

### server-certified-narration-spine

- **L1** — Reject browser-authored narration events.
- **L2** — Emit narration from accepted GameServer events and remove the client reporter.
- **L3** — Certified typed projection, privacy-minimal deduplication, route-policy contraction, integrity counters, and adversarial tests.

### runtime-feature-reachability-closure

- **L1** — Remove the unconsumed Intel HTTP market and random quote fallback.
- **L2** — Retire all verified unreachable feature clusters and reconcile policies/imports.
- **L3** — Source-backed reachability catalog, measurable composition contraction, exact route-policy ceilings, and negative reintroduction tests.

### session-bound-prematch-intelligence

- **L1** — Reject stale async results and clear timers on hide.
- **L2** — Add abort signals, per-task deadlines, and lifecycle tests.
- **L3** — Session receipt, accessible responsive state machine, three-theme desktop/mobile rendered proof, and exact cleanup invariants.

### single-public-route-graph

- **L1** — Import footer links from the existing manifest into the app.
- **L2** — Type the manifest and include app-only external links.
- **L3** — One projection authority, app/static parity checker, digest evidence, and drift mutation tests.

### player-visible-transport-recovery

- **L1** — Replace alert() with the existing modal path.
- **L2** — Render waiting/recovery/overflow state from transport events.
- **L3** — Accessible generation-safe presenter, honest synchronization lifecycle, theme/mobile proof, and adversarial event tests.

### match-bound-first-contact

- **L1** — Remove the lobby auto-open timer and internal-process hero copy.
- **L2** — Gate a lazy tutorial on a match-ready event and add lifecycle tests.
- **L3** — Authoritative match-bound state machine, exactly-once telemetry, lazy chunk/transfer proof, adversarial lifecycle tests, and full rendered-pixel matrix.

### personal-first-extraction-authority

- **L1** — Stop later team Pressure from implying personal capture and convoy action.
- **L2** — Consume contributors and exact actor activity for personal step ownership.
- **L3** — Reconnect-safe personal/team ledger, explicit copy, receipt semantics, adversarial team tests, and rendered proof.

### mastery-doctrine-vault

- **L1** — Expose a static non-power unlock catalog and next threshold.
- **L2** — Add authenticated replay-safe unlock/select with durable entitlement state.
- **L3** — Transactional PostgreSQL/process-local parity, route-policy admission, active coaching identity, concurrency tests, receipts, and rendered choice states.

### actionable-genius-brief-truth

- **L1** — Filter done items from the brief.
- **L2** — Add explicit exhausted and blocked-only states with counts.
- **L3** — Source-derived taxonomy ledger, adversarial sidecar tests, cache/Markdown parity, and canonical brief validation.

### doctor-evidence-sidecar

- **L1** — Move doctor checks into a dedicated sidecar.
- **L2** — Bind the compact summary to the sidecar digest and lazy-load non-green detail.
- **L3** — Atomic write, fail-closed verification, legacy migration, tamper tests, and measured token/byte delta.

### deterministic-match-session-teardown

- **L1** — Call currentGameRunner.stop before nulling it.
- **L2** — Cancel the renderer loop and input timers/listeners.
- **L3** — Ship scoped EventBus rollback, layer disposal, idempotence, and leave/rematch lifecycle tests.

### causal-certified-match-feedback

- **L1** — Expose one bounded reason choice in the feedback UI.
- **L2** — Validate and retain the reason with the certified match row.
- **L3** — Add certified reason cohorts, receipts, accessibility, stale-session tests, and rendered proof.

### truthful-multitab-collision

- **L1** — Remove fake IP, fingerprint, and report claims.
- **L2** — Make detector and countdown lifecycle idempotent.
- **L3** — Ship truth-labeled receipt semantics, accessibility, teardown contracts, and rendered proof.

### retire-browser-authored-remote-microhint

- **L1** — Disable the remote enhancement toggle.
- **L2** — Remove the route and client helper while preserving local hints.
- **L3** — Add reachability tombstones, reservation-contract coverage, telemetry assertions, and bundle verification.

### generation-safe-progression-debrief

- **L1** — Reset requested when a new game binds.
- **L2** — Guard delayed receipts with a game generation.
- **L3** — Add abortable polling, full state reset, consecutive-rematch tests, and lifecycle integration.

### doctrine-guided-rematch-continuity

- **L1** — Display the active Doctrine in the debrief.
- **L2** — Include its server-owned id and brief in the rematch event.
- **L3** — Bind it to generation-safe evidence, tests, accessibility, and theme/mobile proof.

### accessible-account-recovery-state

- **L1** — Replace alert with inline status.
- **L2** — Add pending and duplicate-submit protection.
- **L3** — Ship accessible state-machine tests, modal-session reset, and rendered theme/mobile proof.

### match-safe-blue-green-promotion

- **L1** — Replace hard removal with bounded graceful stop and prove signal receipt.
- **L2** — Add drain/readiness state, reject new games, wait for active games, and fail promotion rather than killing them.
- **L3** — Ship blue/green generation routing, sticky drain, exact-digest receipt, and non-destructive rollback.

### durable-certified-archive-outbox

- **L1** — Return typed success/failure, add a deadline, and stop equating certificate presence with delivery.
- **L2** — Add idempotent bounded retry, explicit certified/incomplete types, and readiness visibility.
- **L3** — Ship a durable PostgreSQL outbox with restart replay, acknowledgements, dead-letter visibility, and receipt-gated consumers.

### authoritative-achievement-rehydration

- **L1** — Hydrate before authenticated read and persist meta unlocks.
- **L2** — Add singleflight hydration, bounded eviction, degraded state, and queued writes.
- **L3** — Make PostgreSQL/read-through authority own counters and unlocks atomically with progression receipts.

### complete-state-scope-inventory

- **L1** — Enumerate all current missing stores and their release criticality.
- **L2** — Require every registered Store owner to appear exactly once through a source-inventory test.
- **L3** — Derive constructors, readiness, schema capabilities, and release posture from one canonical registry.

### redact-turnstile-capability-tokens

- **L1** — Delete the token-bearing log.
- **L2** — Replace it with non-sensitive state metadata and a source-negation test.
- **L3** — Add a centralized browser redaction policy with semantic sensitive-field tests.

### single-bounded-remote-ai-executor

- **L1** — Apply the existing AI deadline and fix the prophecy cache key.
- **L2** — Forward request abort, validate outputs, and expose timeout/cancel counts.
- **L3** — Ship one statically inventoried reserve-execute-validate-receipt helper for every provider edge.

### reachable-doctrine-match-spine

- **L1** — Consume the rematch event and bind its Doctrine receipt to the next generation.
- **L2** — Use the frame in local hints and route emphasis without power changes.
- **L3** — Add a certified Doctrine Review, generation safety, accessibility, and rendered proof.

### cause-bound-local-coach

- **L1** — Remove the false comeback_surge-to-chain_broken mapping.
- **L2** — Emit and render typed reset reasons for timeout, wrong order, and failed denial.
- **L3** — Add explainable action receipts, map focus, post-match evaluation, and certification tests.

### accessible-reroute-decision-matrix

- **L1** — Enforce 44px targets, button type, pressed state, and descriptive labels.
- **L2** — Ship radio-group keyboard behavior and live ETA-risk-reward deltas.
- **L3** — Add Safe/Fast/Value summaries, full disclosure, Doctrine emphasis, and rendered proof.

## Execution Log

### retire-public-obelisk-broker-copy

- Session 95: orphaned internal Obelisk broker removed; deployable-code public-boundary guard passes 1/1.

### project-local-skill-bridge

- Session 95: allowlisted project-root bridges added; exact skill-profile and sample-codebase commands pass; control-plane tests 2/2.

### asset-aware-secret-signal

- Session 95: asset-aware classifier preserves semantic credential blocking; adversarial tests 4/4; full-tree scan reduced 1,974 low-noise findings to 0 with high/medium still 0.

### bounded-identity-introspection

- Session 95: users/@me admission deadline, abort ownership, normalized errors, and timer cleanup implemented; focused identity tests 4/4.

### idempotent-doctor-closeout-format

- Session 95: closeout owns doctor-sidecar formatting; two-pass byte-idempotency and canonical digest regression pass (DoctorEvidence 4/4).

### single-owner-static-assets

- Session 95: resources now use explicit copy ownership and virtual stable URLs; ownership tests 2/2; build 2,201 modules, 47 copy items, 0 retired warnings; pages 10/10 and bundle budgets pass.

### server-certified-narration-spine

- Removed the browser-writable narrator event route and NarratorReporter client mutation.
- Added a typed privacy-minimal projection after GameServer intent admission; rejected intents and caller prose cannot enter narration.
- Added a deterministic certified baseline with optional remote enrichment, a visible aria-live consumer, and adversarial authority tests.
- Contract result: 29 declared/registered mutations, public ingest 4/4.

### runtime-feature-reachability-closure

- Retired fourteen verified unreachable routes across battle narrative, commentary, missions, bot lore/persona, match coach, Intel HTTP market, clan war, and browser narrator ingestion.
- Removed the unreachable ClanWarStore and client helpers while preserving and explicitly cataloging the live micro-coach and progression router.
- Added a source-backed catalog for eight admitted route→transport→consumer chains, fourteen retired-route tombstones, mutation tests, doctor coverage, and a sha256 evidence digest.
- Worker contracted from 2,849 audited lines to 2,420 with a 2,440-line ratchet.

### session-bound-prematch-intelligence

- Added generation ownership, AbortController propagation, a six-second deadline, all-settled projection, stale-result rejection, and exact timer teardown.
- Added accessible loading/degraded/ready states and a 92vw/32rem, 85dvh responsive dialog.
- Focused lifecycle tests prove hide abort and cross-match stale suppression; the three-state desktop/mobile/theme matrix is included in the 90-artifact visual receipt.

### single-public-route-graph

- Created src/shared/PublicRouteGraph.json as the canonical typed route graph and kept public/footer-manifest.json as an exact checked mirror.
- The static shell generator and Lit footer now project the same seven routes, ownership, copyright, attribution, and two classified app-external links.
- Footer checker now validates app consumption, mirror parity, hard-coded-route absence, and a sha256 graph digest.

### player-visible-transport-recovery

- Added an accessible player-visible presenter for connecting, waiting, synchronizing, restored, fatal refusal, and reject-newest overflow states.
- Removed blocking alert(), sanitized fatal reason text, and bound retry/outbox language to exact transport events.
- Reordered transport truth so open is published only after authoritative rejoin and FIFO flush; focused tests prove one zero-depth open event.
- All five states pass desktop/mobile/theme rendered proof.

### match-bound-first-contact

- Removed timer-driven lobby presentation; first contact now lazy-loads only after ControlPanel receives the first authoritative VaultFront status.
- Rewrote the lobby hero around Capture → Convoy → Pressure → Breach and preserved exactly-once shown/advance/skip/complete telemetry.
- Split tutorial, lobby, and leaderboard secondary UI from the initial entry; unchanged Brotli gate is green at 588,569 bytes against 592,619 bytes.
- Focused tutorial, authority, HUD, TypeScript, and bundle contracts pass.

### personal-first-extraction-authority

- Removed every team-state implication into personal capture, convoy, Pressure contribution, and completion evidence.
- Personal capture recovers from exact actor activity or controlled-site status; convoy and Pressure recover from exact actor activity and the server-derived contributor ledger.
- Team threshold/window advances only the explicitly team-labelled Breach step after personal contribution; decisive delivery requires the exact vault_breach_victory actor.
- Completion now requires all five personally truthful steps, with adversarial shared-Pressure tests.

### mastery-doctrine-vault

- Added a server-owned three-Doctrine catalog explicitly limited to coaching and identity, never combat power.
- Added authenticated, rate-limited, schema-validated, replay-safe select-or-unlock with atomic wallet deduction and PostgreSQL/process-local parity.
- Added durable unlock, active-profile, and request-receipt tables plus owned/active/affordability UI and honest insufficient-funds receipts.
- Focused store, router, policy, UI, TypeScript, and 30/30 mutation-route coverage contracts pass.

### actionable-genius-brief-truth

- Extracted one fail-closed genius-brief taxonomy authority and removed completed history from the actionable hit list.
- Added source-derived exhausted and blocked-only tiles with contiguous actionable ranks and an innovation-pack continuation command.
- Mixed, exhausted, blocked-only, malformed, and generator contract tests pass.

### doctor-evidence-sidecar

- Moved full doctor checks into an atomic audits/doctor-latest.json evidence sidecar bound to compact project status by SHA-256.
- Startup now lazy-loads detail only for non-green health, fails closed on missing/tampered evidence, and retains legacy embedded-check compatibility.
- Atomic reload, tamper, unsafe-path, missing, legacy, project-doctor, and startup contracts pass.

### deterministic-match-session-teardown

- Session 96: match construction and runtime teardown now share a generation boundary; pre-start and active runners release worker, transport, renderer canvas/RAF/layers, input, touch, timers, and EventBus listeners exactly once; lifecycle tests pass.

### causal-certified-match-feedback

- Session 96: certified feedback accepts one bounded enumerated causal signal, persists it in the existing retained row, exposes privacy-safe signal cohorts and receipt provenance, and renders accessible 44px choices; focused and full tests pass.

### truthful-multitab-collision

- Session 96: fabricated IP/fingerprint/record/report/suspension claims removed; same-origin storage scope is explicit; detector/listener/countdown lifecycle is idempotent; desktop/mobile all-theme proof passes.

### retire-browser-authored-remote-microhint

- Session 96: unauthenticated remote micro-hint route, client helper, toggle, and provider prompt removed; local deterministic coaching retained; route tombstone, telemetry, reachability, tests, and production build pass.

### generation-safe-progression-debrief

- Session 96: progression singleton resets per game generation, resolves cancelled waits, rejects stale receipts, and repeats across rematches; consecutive-bind and lifecycle coverage pass.

### doctrine-guided-rematch-continuity

- Session 96: active server-owned non-power Doctrine is loaded into the progression debrief and bound into rematch intent with coaching-only policy; generation guards, tests, and rendered proof pass.

### accessible-account-recovery-state

- Session 96: blocking recovery alerts replaced by inline validation/pending/success/error state with duplicate-submit guard and ARIA relationships; focused tests and desktop/mobile all-theme proof pass.

### match-safe-blue-green-promotion

- Session 97: candidate-first health and immutable revision admission now precede bounded incumbent drain; duplicate incumbents fail closed, rollback restores the exact incumbent, and 110 deploy-contract checks pass, including Git executable-mode enforcement for all three deployment scripts.

### durable-certified-archive-outbox

- Session 97: certified/incomplete archive delivery now uses typed receipts, bounded deadlines, leased PostgreSQL outbox retry, idempotency, dead-letter state, restart pumping, and fail-closed configured-database truth; focused tests pass.

### authoritative-achievement-rehydration

- Session 97: achievements singleflight-hydrate before authenticated reads/progression, bound cache lifetime and failure state, persist base/meta unlocks canonically, and pass restart, concurrency, dedupe, degradation, and router tests.

### complete-state-scope-inventory

- Session 97: a source-backed 20-owner registry now derives state-scope integrity/readiness and recursively rejects omitted Store exports; ReplayStore truth is corrected to process-local HMAC integrity.

### redact-turnstile-capability-tokens

- Session 97: the browser no longer logs the Turnstile capability; only non-sensitive completion state remains, protected by a source-negation regression.

### single-bounded-remote-ai-executor

- Session 97: every Worker and narrator provider edge now runs through one deadline/cancellation/outcome executor; request disconnects propagate, output bounds fail closed, prophecy cache identity includes mutator, and focused contracts pass.

### reachable-doctrine-match-spine

- Session 97: certified Doctrine intent persists source/target match identity, binds exactly once to the subsequent live generation, frames local coaching without statistics, and passes generation/UI tests.

### cause-bound-local-coach

- Session 97: deterministic execution publishes typed chain-reset reason/tick/from-step evidence and the coach consumes only that source; fabricated comeback_surge causality is removed.

### accessible-reroute-decision-matrix

- Session 97: reroute projections now use 44px radio/pressed controls, roving arrow/Home/End navigation, and polite live ETA-risk-reward descriptions; focused HUD tests pass.
