<!-- generated-by: render-audit-md.mjs — DERIVED FROM AUDIT_2026-07-25.json; do not hand-edit -->

# Project Audit — VaultFront

> Source of truth: the JSON sidecar. Edit JSON, then re-render.

## Ranked Plan

| #   | Tier | Axis                                                  | Effort | Impact | Innov | Priority | Status | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | :--: | ----------------------------------------------------- | ------ | :----: | :---: | :------: | :----: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 81  |  🔥  | security / retention / ux / organization              | 6h     |   10   |   9   |   96.0   |   ✓    | **certified-participant-rematch-corridor** — Turn rematch into a certified participant-bound continuation. POST /api/rematch/:gameId verifies a play token but immediately joins an existing corridor or clones source configuration without proving that the actor participated in the source match. Knowledge of a game ID is therefore stronger than certified match membership. **Recipe:** Extract a testable RematchRouter; resolve a source-match participation certificate from the live GameServer or archived GameRecord before join/create; reject nonparticipants and uncertified archives; keep privacy-safe participant keys internal; bind the route to the policy manifest; and cover missing identity, nonparticipant, archived participant, live participant, replay, and expiry paths. |
| 82  |  🔥  | retention / security / feature depth / observability  | 5h     |   9    |   9   |   92.0   |   ✓    | **content-addressed-replay-share-contract** — Make every replay share a content-addressed projection of signed evidence. ReplayHighlightStore and the custom clip route generate random nanoid identifiers. The same signed replay therefore produces different share URLs after restart, while custom clip ranges are checked only for ordering and can exceed the replay's signed duration. **Recipe:** Create a versioned ReplayShareContract that verifies replay integrity posture, canonicalizes game/digest/range, bounds ranges to duration, hashes the projection into a stable identifier, and emits a canonical URL. Route automatic highlights and custom clips through it; reject invalid/unsigned production evidence; add restart stability, tamper, boundary, and URL tests.              |
| 83  |  ⚡  | engagement / security / feedback loop / observability | 4h     |   9    |   8   |   86.0   |   ✓    | **lifecycle-bound-prediction-ledger** — Bind spectator predictions to an executable open-match lifecycle. PredictionLeagueRouter authenticates the spectator but passes any syntactically valid gameId to the durable store. The store rejects duplicates and resolved games, yet it cannot distinguish a real open match from an invented ID or a match whose outcome is already visible but not yet resolved. **Recipe:** Inject a fail-closed prediction-admission resolver backed by GameServer lifecycle; admit only real started/not-ended games before outcome visibility; publish an admission reason without leaking private match state; preserve store-level duplicate/resolution locks; and cover invented, lobby, active, ended, duplicate, and storage-unavailable paths.                   |

## Depth Ladder

### certified-participant-rematch-corridor

- **L1** — Check actor membership before creating a rematch.
- **L2** — Check both live and archived source matches and cover nonparticipant/replay paths.
- **L3** — Extract an injected router, bind policy/certificate evidence, keep clone authority server-side, and ratchet Worker composition.

### content-addressed-replay-share-contract

- **L1** — Derive deterministic IDs from game identity and clip bounds.
- **L2** — Bound ranges to manifest duration and test stable URLs.
- **L3** — Bind a versioned content digest to verified replay evidence, centralize URL generation, and make automatic/custom shares independently verifiable.

### lifecycle-bound-prediction-ledger

- **L1** — Reject predictions when no live game exists.
- **L2** — Model lobby/open/closed admission and add direct router tests.
- **L3** — Expose a pure lifecycle admission contract, bind Worker composition, and verify durable resolution cannot race late predictions.

## Execution Log

### certified-participant-rematch-corridor

- L3 shipped: Rematch routes now live behind an injected authorization seam. Join and creation both require a verified actor who appears in the live GameServer or archived GameRecord; replay possession alone cannot authorize configuration cloning. The route preserves server-side private-lobby construction, emits the source evidence class, and has direct missing-actor, nonparticipant, participant, and corridor tests.

### content-addressed-replay-share-contract

- L3 shipped: ReplayShareContract verifies the signed manifest, bounds ranges to recorded duration, derives a versioned content address from replay evidence plus exact projection, canonicalizes share URLs, and rejects unsigned/out-of-range inputs. Automatic highlights and custom clips share the same authority; restart stability, kind separation, tamper, boundary, and route evidence are covered.

### lifecycle-bound-prediction-ledger

- L3 shipped: GameServer exposes a pure non-mutating prediction admission signal and PredictionLeagueRouter requires an injected real-game open-window verdict after authentication but before durable writes. Invented, lobby, and closed identities fail with a non-enumerating response while internal telemetry retains the reason; existing PostgreSQL advisory locks and resolved-game guards remain the race backstop.
