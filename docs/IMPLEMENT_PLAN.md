# Implementation Plan — Session 100

Source: `docs/AUDIT_2026-08-11.json` (six new premise-verified items; 190–195)

## Efficiency order

1. `provider-live-match-readiness-root-fix` (#191) — establish an executable primary-loop baseline before trusting later UI proof.
2. `owned-domain-cloudflare-release-corridor` (#190) — make `vaultfront.io`, staging, provider configuration, and edge truth explicit before release validation.
3. `public-fortune-title-projection` (#192) — extend the existing reward data path before the visual surfaces are refactored.
4. `sidebar-feed-projection-kernel` (#194) — isolate the player-feedback rules and raise their direct coverage.
5. `reroute-preview-panel-extraction` (#195) — finish the deferred ControlPanel split and ratchet its reduced size.
6. `radial-menu-announced-focus-contract` (#193) — complete keyboard/screen-reader behavior, then share one consolidated browser matrix with items 192 and 195.

## Mandatory gates

- Every core-loop change records a loop-tightness measurement note in `DECISIONS.md`.
- No provider, launch, retention, revenue, human-preference, or DNS claim is inferred from local code.
- Every UI-visible change receives real-browser proof across VaultFront, Light, and Competitive themes at desktop and mobile before closeout.
- Audit rows move to `shipped` only after focused behavior verification and an execution-log entry.
- The live-match test keeps its 60-second readiness assertion and remains included in both Playwright projects.
- Cloudflare and GitHub credentials flow only through the Studio secrets gateway and stdin; raw values never enter output, files, or command arguments.
- Production promotion requires the exact staging digest and a live health/revision observation.
- No package installation is planned; any surprise dependency request must pass the package-trust gate first.
