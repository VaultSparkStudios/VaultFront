# Session 109 Implementation Plan

Source: `docs/AUDIT_2026-08-25.json`

1. Restore the project-proven release verification surfaces overwritten by the
   Studio propagation commit, preserve the new maintenance command, and ship
   the regression evidence to Studio Ops through Ark (#243).
2. Prove the repaired revision on stable staging (#244): immutable deployment,
   exact `commit.txt`, product contracts, nine rendered theme/viewport cells,
   signed observation evidence, and a matching production-promotion dry run.
3. Close the current-image rollback gate without bypassing provenance: retain a
   fresh known-good `main` deployment, deploy the final closeout revision from
   `main`, validate rollback intent, then observe rollback and restoration of
   those two distinct immutable images.
4. Run the full verification and release-gate surface. Production may mutate
   only when canonical admission is all green; missing Zoho reply identity,
   authenticated Obelisk, genuine three-human Alpha, live positive revenue, or
   exact-artifact founder approval remain fail-closed (#233–237).
5. Complete the Session 109 write-back, secret scan, doctor/security checks,
   direct-to-`main` commits, exact-SHA provider checks, final staging proof, Ark
   impact summary, and session-lock cleanup.

Core-loop measurement note: this session changes release trust and availability,
not Capture → Convoy → Pressure → Breach → decisive delivery behavior. Gameplay,
reward, and balance changes remain inadmissible until genuine authenticated human
evidence identifies a concrete loop defect.
