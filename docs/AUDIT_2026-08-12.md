<!-- generated-by: render-audit-md.mjs — DERIVED FROM AUDIT_2026-08-12.json; do not hand-edit -->

# Project Audit — VaultFront

> Source of truth: the JSON sidecar. Edit JSON, then re-render.

## Ranked Plan

| #   | Tier | Axis                                       | Effort | Impact | Innov | Priority | Status | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | :--: | ------------------------------------------ | ------ | :----: | :---: | :------: | :----: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 196 |  🔥  | feature depth / reliability / security     | 7h     |   10   |   8   |  112.0   |   ✓    | **same-origin-worker-api-plane** — Bridge the public master to worker-owned project APIs without SPA fallthrough. Live staging returns status 200 text/html for /api/leaderboard, /api/clans/leaderboard, /api/tournaments, Prediction League, and Match Oracle. Those handlers exist only on workers while nginx sends unprefixed traffic to Master, whose wildcard route returns index.html. **Recipe:** Add one bounded master proxy before SPA fallback; preserve canonical game-id sharding for match-bound routes, use a stable control-plane shard for globally durable stores, retain headers/status/body limits, fail JSON-503 on upstream loss, and cover routing plus content type. Core-loop step: post-match/meta input → persistent commander-world fantasy → progression/social reward → next match. |
| 197 |  🔥  | security / feedback loop / efficiency      | 5h     |   9    |   7   |   70.9   |   ✓    | **privacy-safe-singleflight-playtest-pulse** — Publish playtest learning without exposing early-cohort behavior. The public pulse route exposes exact one-to-four actor/session/event counts, rates, freshness, and recent behavior despite /stats.json declaring a five-person small-count threshold. Master and Worker also duplicate the full durable summary query on every read. **Recipe:** Extract one TTL/single-flight summary service; invalidate after writes; project a public schema that bands one-to-four actors and suppresses rates/timeline/freshness while keeping the authenticated operator view exact. Core-loop step: playtest action → measured loop fantasy → privacy-safe learning reward → tuned next session.                                                                           |
| 198 |  ⚡  | gamification / fairness / organization     | 5h     |   8    |   7   |   46.7   |   ✓    | **reproducible-playlist-entropy** — Make public playlist randomness injectable, unbiased, and reproducible. MapPlaylist mixes Math.random, a Date.now seeded generator, and sort(() => random - .5). Tests cannot reproduce a live special lobby, and comparator shuffling is biased. **Recipe:** Inject one entropy source, replace comparator shuffling with Fisher-Yates, route every map/team/modifier roll through it, expose a bounded selection receipt for operator diagnostics, and prove same-seed equality plus different-seed variance. Core-loop step: queue input → surprising fair battlefield fantasy → trusted match reward → replayable tuning.                                                                                                                                                   |
| 199 |  🔥  | ai integration / feedback loop / retention | 6h     |   9    |   8   |   67.5   |   ✓    | **certified-local-coach-baseline** — Keep certified coaching alive when remote AI is unavailable. The Coach tab returns 503 and then 'No debrief moments available' whenever the remote budget reservation is unavailable. A certified match therefore loses its learning reward at the exact cost-neutral baseline. **Recipe:** Derive two or three transparent moments from the requester’s certified intent timeline; label them deterministic replay analysis; return them before optional remote enhancement and fall back on provider error/budget denial. Never claim unobserved causality. Core-loop step: certified result → reflective commander fantasy → actionable learning reward → improved rematch.                                                                                                 |
| 200 |  ⚡  | ux / accessibility                         | 4h     |   7    |   5   |   27.1   |   ✓    | **postmatch-mobile-action-floor** — Give post-match and prediction actions a real mobile interaction floor. AI tabs use py-1, coach uses py-2, several debrief actions use py-0.5/py-1, and Prediction League declares 2.5rem. These important return-loop controls miss the 44px mobile target floor. **Recipe:** Apply one 44px minimum action primitive with visible focus to the touched WinModal, ProgressionDebrief, and Prediction League controls; preserve compact layout; verify all themes at desktop and mobile. Core-loop step: result input → readable reward fantasy → confident next-action reward → continued session.                                                                                                                                                                             |
| 201 |  🔥  | release / automation / security            | 5h     |   9    |   6   |   60.8   |   ✓    | **staging-product-contract-smoke** — Make staging prove product contracts, not only process health. Deploy verifies /_health and /commit.txt. It did not notice that project API requests returned the SPA document with 200. Obelisk and dual-audience files are likewise outside the exact-revision attestation. **Recipe:** Add a reusable origin smoke script that checks JSON content types/statuses for public and unauthenticated project routes, Obelisk redirect host and cookie posture, agents/llms availability, revision, and health; run it before attestation and attach its JSON receipt. Core-loop step: release input → trustworthy live-game fantasy → verified playable reward → safe Alpha iteration.                                                                                          |
| 202 |  ⚡  | observability / ux / dual-audience         | 4h     |   8    |   7   |   46.7   |   ✓    | **live-analytica-homepage-pulse** — Turn public Alpha evidence into one honest homepage and Analytica feed. The live CANON-054 checker revealed that VaultFront's deep stats page predated the newer homepage-showcase, freshness, and Analytica Feed v1 requirements. **Recipe:** Make one precomputed aggregate feed serve the homepage tile, deep stats page, and agent/founder consumers; poll at the declared cadence, mark stale or unavailable truth visibly, and keep small cohorts private.                                                                                                                                                                                                                                                                                                                |
| 203 |  🔥  | release / supply-chain / reliability       | 1h     |   8    |   4   |   48.0   |   ✓    | **supported-node-runtime-floor** — Run CI on the runtime floor the locked dependency graph actually supports. Exact-revision CI exposed intersecting sanitize-html Node >=22.12 and ESLint Node >=22.13 engine contracts while the repository still advertised and ran Node 20. **Recipe:** Raise the repository engine floor and every core CI/brief job to one exact supported Node 22.13 runtime, retain the Node 24 production image, and verify workflow contracts.                                                                                                                                                                                                                                                                                                                                            |

## Depth Ladder

### same-origin-worker-api-plane

- **L1** — Reject unknown /api routes as JSON rather than HTML.
- **L2** — Proxy global project APIs to a stable healthy worker.
- **L3** — Preserve game-id sharding, headers, bounded failures, and direct integration tests.

### privacy-safe-singleflight-playtest-pulse

- **L1** — Remove recent-event timelines from the public response.
- **L2** — Band small cohorts consistently with /stats.json.
- **L3** — Add shared TTL/single-flight computation, write invalidation, and privacy regression tests.

### reproducible-playlist-entropy

- **L1** — Replace comparator shuffle with Fisher-Yates.
- **L2** — Inject all playlist entropy and add deterministic tests.
- **L3** — Add non-secret selection receipts and distribution sanity proof.

### certified-local-coach-baseline

- **L1** — Return two certified timeline moments instead of 503.
- **L2** — Use requester-bound intent phases and transparent heuristic labels.
- **L3** — Make remote coaching an optional enhancement with cache/receipt and fail-safe local fallback.

### postmatch-mobile-action-floor

- **L1** — Raise primary post-match actions to 44px.
- **L2** — Cover tabs, secondary actions, focus-visible, and Prediction League.
- **L3** — Add measured mobile browser proof across every theme and touched state.

### staging-product-contract-smoke

- **L1** — Assert pulse JSON and reject HTML API fallthrough.
- **L2** — Cover auth redirect, agent files, health, and revision.
- **L3** — Emit and upload a hash-bound product smoke receipt in staging deploy.

### live-analytica-homepage-pulse

- **L1** — Declare Feed v1, refresh, and showcase metadata.
- **L2** — Render a theme-aware homepage pulse from the same feed with stale/unavailable states.
- **L3** — Deepen the stats page beyond the tile, preserve privacy/unmeasured semantics, and verify the live canon checker.

### supported-node-runtime-floor

- **L1** — Rerun the failed job without changing its unsupported runtime.
- **L2** — Upgrade only the failing test job.
- **L3** — Align package engines and every CI consumer on an exact supported floor.

## Execution Log

### same-origin-worker-api-plane

- Added a bounded Master-to-worker API proxy before SPA fallback, with stable control-plane routing, canonical game-id sharding, header/status preservation, JSON 503 failures, and a 15-second upstream timeout.
- Covered global, query-bound, body-bound, and path-bound routing plus non-SPA response behavior in WorkerApiProxy tests.

### privacy-safe-singleflight-playtest-pulse

- Extracted a five-second TTL single-flight summary service with explicit write invalidation in both Master and Worker.
- Suppressed one-to-four actor exact counts, rates, freshness, evidence, and timelines from the public projection while preserving a truthful withheld-state client presentation and regression tests.

### reproducible-playlist-entropy

- Routed all playlist entropy through one injectable validated source and replaced comparator sorting with Fisher-Yates.
- Added deterministic same-seed/different-seed tests and a defensive, non-secret SHA-256 selection receipt carrying the bounded operator diagnostics promised by L3.

### certified-local-coach-baseline

- Added requester-bound early/mid/late certified replay moments as a deterministic local baseline that never claims unobserved causality.
- Extracted CoachDebriefRouter, made remote generation an optional budgeted enhancement, and retained the local result on budget denial or provider failure while staying inside Worker composition budgets.

### postmatch-mobile-action-floor

- Raised touched postmatch, progression, coach, story, share, and Prediction League actions to a 44px minimum target with visible keyboard focus.
- Added the live prediction-league proof state and passed the regenerated 120-artifact desktop/mobile matrix across VaultFront, Light, and Competitive themes with a fresh hash-bound receipt.

### staging-product-contract-smoke

- Added an exact-revision origin smoke for health, revision, privacy-safe pulse JSON, worker API non-SPA behavior, Obelisk unauthenticated/PKCE posture, and both agent-facing files.
- Placed the smoke before staging attestation, uploaded its self-digested receipt, and bound that receipt into the run attestation so process health can no longer mask product-contract failure.

### live-analytica-homepage-pulse

- Upgraded the descriptor to Analytica Feed v1 with project identity, a one-day poll contract, aggregate-only privacy, a curated three-metric showcase, and six deeply defined metrics.
- Added a theme-aware homepage Alpha Pulse that reads only the shared static feed, labels unavailable and stale truth explicitly, and links into the deeper stats analysis; CANON-054 now conforms with zero findings.

### supported-node-runtime-floor

- Raised package.json and package-lock.json to Node >=22.13 <25 and all seven CI plus brief-validation setup-node consumers to exact 22.13.0.
- Revalidated 133 deploy contracts, 12 hosted workflow contracts, and focused release/deploy regressions before republishing.
