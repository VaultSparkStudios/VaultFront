<!-- generated-by: render-audit-md.mjs — DERIVED FROM AUDIT_2026-08-03.json; do not hand-edit -->

# Project Audit — VaultFront

> Source of truth: the JSON sidecar. Edit JSON, then re-render.

## Ranked Plan

| #   | Tier | Axis                                                     | Effort | Impact | Innov | Priority | Status | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | :--: | -------------------------------------------------------- | ------ | :----: | :---: | :------: | :----: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 125 |  🔥  | security / process / observability / ecosystem           | 2h     |   10   |   8   |  120.0   |   ✓    | **sibling-capability-map-authority** — Make capability discovery consume the canonical sibling map. scripts/lib/secrets.mjs merges sibling secret values but binds CAP_MAP_PATH only to the local repo. With no local map, /start reports the dishonest 0/0 capabilities ready even though Studio Ops has a healthy catalog. **Recipe:** Load the sibling CAPABILITY_MAP.json as the base authority, merge any local project override by capability, fail loudly on corruption at either layer, and add isolated tests for sibling-only, local override, and corrupt-map behavior. Re-run real discovery and ship the producer pattern to Studio Ops via Ark.                                                                        |
| 126 |  🔥  | release / security / observability / automation          | 5h     |   10   |   8   |   85.5   |   ✓    | **provenance-bound-staging-promotion** — Replace caller-equal digests with run-bound staging attestation. promote.yml accepts image_digest and staging_evidence_digest as manual inputs and proves only format plus equality. An operator can enter the same arbitrary digest twice without a successful staging workflow, commit, health check, or artifact. **Recipe:** Have the staging workflow emit a canonical hash-bound attestation after deployed revision and health verification. Promotion accepts only a staging run ID, downloads that run's artifact from the same repository, verifies workflow/conclusion/run/SHA/origin/freshness/digest semantics, and derives the immutable image digest from admitted evidence. |
| 127 |  🔥  | reliability / security / release / observability         | 6h     |   10   |   7   |   70.0   |   ✓    | **production-durability-admission** — Make durable PostgreSQL a staging and production admission invariant. deploy.sh and both deploy workflows omit DATABASE_URL, while pool.ts treats absence as an allowed process-local fallback and master /_health checks only worker quorum. A release can report healthy while losing certified state on restart. **Recipe:** Require and transport DATABASE_URL for non-dry-run staging/production, apply the idempotent schema before traffic, aggregate worker database posture into master readiness, and make canonical health fail when release-critical persistence is absent or failed. Preserve process-local fallback only for explicit development.                               |
| 128 |  🔥  | observability / release / organization / token reduction | 3h     |   9    |   7   |   81.4   |   ✓    | **single-release-gate-catalog** — Give runtime and build evidence one release-gate authority. generate-release-evidence includes health, rollback, and revenue gates while ReleaseEvidenceContract exposes an older seven-gate list. Runtime readiness can omit blockers that static evidence correctly requires. **Recipe:** Create one typed data catalog consumed by runtime and build tooling, preserve semantic validators for health/rollback/revenue, and add an equality/tamper contract proving both projections expose the same gate IDs and labels.                                                                                                                                                                       |
| 129 |  🔥  | game loop / reliability / UX / performance               | 5h     |   9    |   8   |   77.0   |   ✓    | **ordered-bounded-transport-recovery** — Make multiplayer intent delivery ordered, bounded, and retry-safe. Transport drops intents unless the socket is already open, may call send while CONNECTING or CLOSING, flushes its buffer with pop() in reverse order, and reconnects immediately without backoff. **Recipe:** Introduce a bounded FIFO outbox, explicit connection state, exponential backoff with deterministic jitter injection, generation-safe timers, reset-on-open, intentional-shutdown suppression, and typed connection-state events. Prove FIFO, bounds, reconnect, leave, and stale-socket behavior with fake WebSockets and timers.                                                                          |
| 130 |  🔥  | feedback loop / observability / UX / accessibility       | 4h     |   9    |   7   |   58.5   |   ✓    | **balance-bound-execution-chain-hud** — Bind execution-chain HUD truth to the active balance authority. execution_rush doubles the chain window to 3000 ticks and reward to 1.5x, but VaultFrontLayer always divides by 1500 and celebrates 1.2x. The live HUD drains twice as fast and states the wrong reward. **Recipe:** Carry authoritative duration and multiplier through the status projection, render only those values, add normal and execution-rush tests plus an accessible text mirror, and ensure surge motion respects reduced-motion settings. Capture and inspect desktop/mobile/theme rendered evidence.                                                                                                          |
| 131 |  🔥  | speed / organization / capital / observability           | 4h     |   8    |   7   |   65.0   |   ✓    | **single-build-ci-artifact-fanout** — Build once and fan out one exact CI artifact. ci.yml installs seven times and runs build-prod three times. The build job uploads only static/index.html, so bundle and release-evidence jobs rebuild instead of verifying the same immutable output. **Recipe:** Upload the complete static artifact plus manifest digest once, download and verify it for bundle checks, and make the post-verification fan-in project evidence over that exact artifact without rebuilding. Add digest mismatch tests and preserve exact-revision provenance.                                                                                                                                                |
| 132 |  🔥  | security / release / dependency hygiene                  | 4h     |   9    |   6   |   62.7   |   ✓    | **immutable-workflow-and-host-trust** — Make delivery dependencies and SSH host identity immutable. Most workflows use mutable action tags, release images use mutable base/service tags, and deploy/promote trust ssh-keyscan output collected during the release. **Recipe:** Pin official actions by full commit SHA with readable version comments, pin release-stage container images by digest, require protected known_hosts evidence rather than runtime trust-on-first-use, consolidate SSH setup, and add a checker that rejects mutable release inputs.                                                                                                                                                                   |
| 133 |  🔥  | observability / reliability / release / automation       | 4h     |   8    |   8   |   74.3   |   ✓    | **release-identity-telemetry-lifecycle** — Make telemetry revision-identifiable and shutdown-safe. Telemetry emits upstream service name openfront and hard-coded version 1.0.0; metric/log providers expose no bounded shutdown path, so canary/rollback signals cannot identify the VaultFront revision and final intervals may be lost. **Recipe:** Derive service name, version, environment, and git revision from canonical runtime identity; expose idempotent forceFlush/shutdown handles; drain them within a bounded signal budget before worker exit; and verify identity and failure-tolerant lifecycle behavior.                                                                                                        |
| 134 |  ⚡  | feature depth / UX / security / token reduction          | 3h     |   8    |   6   |   48.0   |   ✓    | **retire-phantom-tutorial-authority** — Retire the unreachable server tutorial that teaches phantom mechanics. TutorialOrchestrator teaches manual convoy send, unit-elimination chains, and four-player tower capture, while the authoritative game uses automatic launch and Capture-to-Deliver-to-Jam. It has server routes but no client consumer or focused suite. **Recipe:** Remove the unreachable routes/state machine and source-map claim, keep FirstExtractionQuest as the only player tutorial authority, add a reachability contract forbidding tutorial routes without a mounted consumer, and update route policy plus public capability evidence.                                                                   |

## Depth Ladder

### sibling-capability-map-authority

- **L1** — Fall back to the sibling map when the local map is absent.
- **L2** — Merge base and local definitions with corruption signaling.
- **L3** — Layered authority, deterministic tests, honest audit output, and Ark producer feedback.

### provenance-bound-staging-promotion

- **L1** — Emit and download a staging artifact.
- **L2** — Bind run, repository, commit, origin, health, and image digest.
- **L3** — Fail-closed verifier, workflow provenance checks, tamper tests, dry-run contract, and runbook alignment.

### production-durability-admission

- **L1** — Pass and require DATABASE_URL in release workflows.
- **L2** — Add schema migration and worker persistence posture.
- **L3** — Fail-closed release health, exact tests, dry-run contract, and runbook/capability documentation.

### single-release-gate-catalog

- **L1** — Add the missing runtime gate names.
- **L2** — Move names and labels into one shared catalog.
- **L3** — Shared typed authority plus semantic parity and mutation-sensitive tests.

### ordered-bounded-transport-recovery

- **L1** — Use FIFO and buffer every non-open send.
- **L2** — Bound the outbox and add reconnect backoff.
- **L3** — Generation-safe lifecycle, observable states, deterministic tests, and no duplicate sends.

### balance-bound-execution-chain-hud

- **L1** — Correct mutator-specific constants.
- **L2** — Carry values in status state and add tests.
- **L3** — Authority-derived projection, accessible mirror, reduced-motion semantics, and rendered visual proof.

### single-build-ci-artifact-fanout

- **L1** — Upload/download the complete static artifact.
- **L2** — Reuse it for bundle verification.
- **L3** — Hash-bound fan-out, evidence-only projector, contract tests, and workflow gate integration.

### immutable-workflow-and-host-trust

- **L1** — Replace ssh-keyscan with protected known_hosts.
- **L2** — Pin GitHub Actions and add a checker.
- **L3** — Pin actions and release images, validate host evidence, and gate CI with mutation tests.

### release-identity-telemetry-lifecycle

- **L1** — Correct service identity and revision fields.
- **L2** — Add provider shutdown handles.
- **L3** — Shared identity resource, bounded graceful flush, exact tests, and release evidence binding.

### retire-phantom-tutorial-authority

- **L1** — Remove the phantom copy.
- **L2** — Remove the dead route/state surface.
- **L3** — Retire the surface, reconcile policy/evidence, and add a reachability regression gate.

## Execution Log

### sibling-capability-map-authority

- Layered the canonical sibling Studio Ops capability catalog beneath deterministic project-local overrides and made corruption fail loudly at either authority layer.
- Real discovery now returns the canonical catalog instead of 0/0; focused protocol tests, TypeScript, lint, and verify:contracts passed.
- Shipped producer feedback through signed Ark cargo 01JV4OG59D6AF2721E56545A1B.

### provenance-bound-staging-promotion

- Staging now emits a canonical run/repository/SHA/origin/health/image/freshness attestation artifact; promotion accepts a staging run ID and derives the admitted image digest.
- The verifier rejects workflow, conclusion, repository, revision, run, freshness, health, origin, and digest tampering without inventing a staging observation.
- Focused staging-attestation tests and the 78-check deploy contract passed.

### production-durability-admission

- Non-development deploys now require DATABASE_URL, apply the idempotent schema before traffic, and carry the secret through deploy/update workflow boundaries.
- Worker database posture is aggregated through IPC into canonical master health so staging/production cannot report healthy on process-local persistence.
- Development-only fallback remains explicit; heartbeat, deploy, shell-syntax, TypeScript, and focused durability checks passed.

### single-release-gate-catalog

- Created one JSON release-gate authority with shared Node and TypeScript projections, semantic evaluators, and a catalog fingerprint.
- Runtime readiness now admits the same 11 gates as generated evidence, including health, rollback, revenue, and authenticated human Alpha evidence.
- Parity, duplicate-ID, semantic mutation, fingerprint tamper, focused contract, TypeScript, and lint verification passed.

### ordered-bounded-transport-recovery

- Transport now uses a reject-newest bounded FIFO outbox, explicit observable states, synchronized join/rejoin, deterministic exponential backoff, and generation-safe lifecycle timers.
- Adversarial review fixed premature retry reset, protocol-refusal retry leakage, and stale sockets that never emit close; leave suppresses recovery and clears queued intents.
- Fake-WebSocket/timer regressions, TypeScript, lint, composition, and the full contract suite passed.

### balance-bound-execution-chain-hud

- Execution status now carries authority-derived chain duration and reward multiplier; Canvas timer/reward copy renders normal 1500/1.2 and rush 3000/1.5 truth with an atomic polite screen-reader mirror.
- Surge and outcome animation freeze under reduced motion; visual inspection found and fixed light-theme completion contrast with an executable 4.5:1 gate.
- CANON-053 passed with 36 hash-bound play/settings/postmatch/execution captures across dark, light, competitive and desktop/mobile; receipt 7f53c3017148ab5915fcd0ce5baa97181e26e823b033d1019e50ec1276e5c97f.

### single-build-ci-artifact-fanout

- CI builds the complete static artifact once, writes a canonical content manifest, uploads one immutable artifact, and verifies that exact payload in bundle and release-evidence consumers.
- Digest mismatch and workflow rebuild regressions are mutation-tested; release evidence is projected only after exact artifact verification.
- Build-artifact, CI fan-out, deploy, TypeScript, and full contract checks passed.

### immutable-workflow-and-host-trust

- Official GitHub Actions are full-SHA pinned and release SSH consumes validated protected DEPLOY_KNOWN_HOSTS evidence with no runtime trust-on-first-use.
- Official Node and PostgreSQL multi-architecture manifests were inspected without pulling layers, pinned by sha256 digest, and bound to registry/source-revision evidence with tamper tests.
- Release-mode input trust now reports releaseReady true; trust, known-hosts, workflow, and deploy contracts passed.

### release-identity-telemetry-lifecycle

- Metrics and logs now share VaultFront service name, package version, environment, and exact git revision from one canonical telemetry identity.
- Providers expose idempotent forceFlush/shutdown handles and Worker drains them within a bounded signal budget while tolerating exporter failures.
- Telemetry identity/lifecycle tests, TypeScript, and the full contract suite passed.

### retire-phantom-tutorial-authority

- Removed TutorialOrchestrator, its three unreachable Worker routes, both mutation policies, and the stale source-map capability claim.
- Added TutorialAuthorityReachability.test.ts to prove FirstExtractionQuest remains mounted and no server tutorial authority or API can reappear without explicit contract change.
- Verified directly: 2 focused files, 18 tests passed.
